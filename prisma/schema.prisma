generator client {
    provider = "prisma-client"
    output = "../src/main/generated/prisma"
    moduleFormat = "cjs"
}

datasource db {
    provider = "sqlite"
}

model Client {
    id       Int     @default(autoincrement()) @id
    name     String
}
